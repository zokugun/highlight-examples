// fold by language
let foo = {
	x: 0,
	y: 0
};

let zeta = foo.x * foo.y;

//{ my first collapsible
let zeta1 = foo.x * foo.y;
//}

console.log(zeta, zeta1)

//{ my second collapsible
let zeta2 = foo.x * foo.y;
//}