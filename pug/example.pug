a.button

div(class='div-class', (click)='play()')
div(class='div-class' '(click)'='play()')

mixin article(title)
  .article
    .article-wrapper
      h1= title
      if block
        block
      else
        p No content provided

+article('Hello world')

+article('Hello world')
  p This is my
  p Amazing article

#title #{this.props.title}  